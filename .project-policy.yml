policy_profile: stage-driven
policy_version: 1.0.14
conventions_engine:
  repo: hamanpaul/paulsha-conventions
preflight:
  steps:
    - name: openspec
      kind: validation
      argv: ["openspec", "validate", "--all", "--strict"]
      when_path_exists: "openspec"
      timeout_seconds: 300
    - name: tests
      kind: tests
      argv: ["./scripts/preflight-tests.sh"]
      timeout_seconds: 1800
