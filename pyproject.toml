[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "paulshaclaw"
version = "0.1.0"
requires-python = ">=3.10"
description = "paulshaclaw agent workflow system"

[tool.setuptools.packages.find]
where = ["."]
include = ["paulshaclaw*"]
