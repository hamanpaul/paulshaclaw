# macOS / Windows
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Python cache / tooling
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# 測試產物：stage7 deploy 測試會在此建假 HOME 後清掉（#285）。
# 這批檔案曾於 #125 隨遷移刀誤入版控，導致每次跑測試 git status 都髒。
.test-artifacts/

# Build / release artifacts（python -m build 輸出）
build/
dist/


# Virtual environments
.venv/
venv/
env/

# Local env / secrets
.env
.env.*
!.env.example
.claude/settings.local.json

# Logs / temp
*.log
tmp/
.psc_tmp/

# External reference repos (Stage 0)
ref/
.worktrees/
.test-work/

# Generated artifacts (brag / demo videos, hyperframes composition output)
brag-output/

# cortex dogfood sandbox (isolated coordinator/control/specs; never committed)
.dogfood/
